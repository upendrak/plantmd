`model_vgg16_2.hdf5` is not committed to this repo. It's downloaded
automatically from Hugging Face Hub (see `src/plantmd/model.py`) the first
time the app runs, and cached here afterward.

`disease_description.csv` is committed normally — it's small text data.
