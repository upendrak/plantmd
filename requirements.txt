# Streamlit Community Cloud and Hugging Face Spaces both auto-detect Python
# dependencies from a root-level requirements.txt, not from pyproject.toml.
# `-e .` installs this project itself (pulling in the pinned dependencies
# declared in pyproject.toml, the actual single source of truth), so there is
# nothing here to keep in sync by hand.
-e .
